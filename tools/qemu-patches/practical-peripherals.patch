diff --git a/hw/mcs51/stc32g.c b/hw/mcs51/stc32g.c
--- a/hw/mcs51/stc32g.c
+++ b/hw/mcs51/stc32g.c
@@ -95,6 +95,19 @@
     if (!sysbus_realize(SYS_BUS_DEVICE(s->uart), errp)) {
         return;
     }
+    /* Add common GPIO for exact-memory profiles without enabling the
+     * unrelated ADC, SPI, I2C, DSP or other full-machine peripherals.
+     * Only the common P0-P7 bank is modeled by this GPIO device.
+     */
+    if (!s->full_model) {
+        s->gpio = qdev_new(TYPE_STC32G_GPIO);
+        object_property_add_child(OBJECT(s), "gpio", OBJECT(s->gpio));
+        object_property_set_link(OBJECT(s->gpio), "cpu", OBJECT(&s->cpu),
+                                 &error_abort);
+        if (!sysbus_realize(SYS_BUS_DEVICE(s->gpio), errp)) {
+            return;
+        }
+    }
     if (s->full_model) {
         stc32g_soc_create_full_children(s);
         object_property_set_link(OBJECT(s->dsp), "cpu", OBJECT(&s->cpu),
@@ -119,6 +132,12 @@
                                     &s->exec_code_alias);
     }
     memory_region_add_subregion(sysmem, s->flash_base, &s->flash);
+    {
+        DeviceState *pwm = qdev_new("stc-advanced-pwm");
+        object_property_add_child(OBJECT(s), "pwm", OBJECT(pwm));
+        if (!sysbus_realize(SYS_BUS_DEVICE(pwm), errp)) return;
+        sysbus_mmio_map(SYS_BUS_DEVICE(pwm), 0, 0x7efeb0);
+    }
     memory_region_add_subregion(sysmem, MCS251_SFR_PHYS_BASE, &s->cpu.sfr);
     memory_region_add_subregion(sysmem, MCS251_DISABLED_PHYS_BASE,
                                 &s->cpu.disabled);
@@ -142,6 +161,16 @@
                         STC32G_TFPU_XFR_BASE);
     }
 
+    if (!s->full_model) {
+        sysbus_mmio_map_overlap(SYS_BUS_DEVICE(s->gpio), 0,
+                                MCS251_SFR_PHYS_BASE, 1);
+        for (i = 0; i < 2; i++) {
+            qdev_connect_gpio_out_named(s->gpio, "int-line", i,
+                qdev_get_gpio_in_named(s->timer, "gate", i));
+            qdev_connect_gpio_out_named(s->gpio, "gpio-out", 3 * 8 + 4 + i,
+                qdev_get_gpio_in_named(s->timer, "counter", i));
+        }
+    }
     for (i = 0; i < 4; i++) {
         sysbus_connect_irq(SYS_BUS_DEVICE(s->timer), i,
                            qdev_get_gpio_in(DEVICE(&s->cpu), i));
diff --git a/hw/timer/stc32g_timer.c b/hw/timer/stc32g_timer.c
--- a/hw/timer/stc32g_timer.c
+++ b/hw/timer/stc32g_timer.c
@@ -158,15 +158,13 @@
 
     switch (source) {
     case MCS251_IRQ_INT0:
-        level = FIELD_EX8(env->tcon, TCON, IT0) ?
-            FIELD_EX8(env->tcon, TCON, IE0) : !s->gate[0];
+        level = FIELD_EX8(env->tcon, TCON, IE0);
         break;
     case MCS251_IRQ_TIMER0:
         level = FIELD_EX8(env->tcon, TCON, TF0);
         break;
     case MCS251_IRQ_INT1:
-        level = FIELD_EX8(env->tcon, TCON, IT1) ?
-            FIELD_EX8(env->tcon, TCON, IE1) : !s->gate[1];
+        level = FIELD_EX8(env->tcon, TCON, IE1);
         break;
     case MCS251_IRQ_TIMER1:
         level = FIELD_EX8(env->tcon, TCON, TF1);
@@ -587,34 +585,24 @@
 {
     Stc32gTimerState *s = opaque;
     CPUMCS251State *env = &s->cpu->env;
+    bool changed = s->gate[n] != !!level;
     bool falling = s->gate[n] && !level;
-    bool edge_triggered;
-
-    /*
-     * P3.2/P3.3 are both the timer gate inputs and the dedicated external
-     * interrupt pins.  Keep TCON flag generation and its IRQ output in this
-     * device so each CPU interrupt input has only one qirq driver.
+    bool falling_only = n ? FIELD_EX8(env->tcon, TCON, IT1) :
+                            FIELD_EX8(env->tcon, TCON, IT0);
+
+    /* STC8/STC32 ITn=0 latches BOTH edges, not a classic low level.
+     * The latched request survives a pin returning high before vector entry.
+     * Holding either level must never retrigger after acknowledgement.
      */
     stc32g_timer_sync(s, n);
-    s->gate[n] = level;
-    edge_triggered = n ? FIELD_EX8(env->tcon, TCON, IT1) :
-                         FIELD_EX8(env->tcon, TCON, IT0);
-    if (edge_triggered && falling) {
+    s->gate[n] = !!level;
+    if (falling || (changed && !falling_only)) {
         if (n) {
             env->tcon = FIELD_DP8(env->tcon, TCON, IE1, 1);
         } else {
             env->tcon = FIELD_DP8(env->tcon, TCON, IE0, 1);
         }
-        stc32g_timer_update_irq(s, n ? MCS251_IRQ_INT1 :
-                                      MCS251_IRQ_INT0);
-    } else if (!edge_triggered) {
-        if (n) {
-            env->tcon = FIELD_DP8(env->tcon, TCON, IE1, !level);
-        } else {
-            env->tcon = FIELD_DP8(env->tcon, TCON, IE0, !level);
-        }
-        stc32g_timer_update_irq(s, n ? MCS251_IRQ_INT1 :
-                                      MCS251_IRQ_INT0);
+        stc32g_timer_update_irq(s, n ? MCS251_IRQ_INT1 : MCS251_IRQ_INT0);
     }
     s->last_ns[n] = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
     stc32g_timer_schedule(s, n);
@@ -804,3 +792,5 @@
 }
 
 type_init(stc32g_timer_register_types)
+
+#include "stc_advanced_pwm.inc"
diff --git a/target/mcs51/cpu.c b/target/mcs51/cpu.c
--- a/target/mcs51/cpu.c
+++ b/target/mcs51/cpu.c
@@ -63,10 +63,8 @@
         cpu->irq_priority[irq] = extract8(env->iph, irq, 1) * 2 +
                                  extract8(env->ip, irq, 1);
     }
-    cpu->irq_auto_clear[MCS251_IRQ_INT0] =
-        FIELD_EX8(env->tcon, TCON, IT0);
-    cpu->irq_auto_clear[MCS251_IRQ_INT1] =
-        FIELD_EX8(env->tcon, TCON, IT1);
+    cpu->irq_auto_clear[MCS251_IRQ_INT0] = true;
+    cpu->irq_auto_clear[MCS251_IRQ_INT1] = true;
     mcs251_cpu_update_interrupt_request(cpu);
 }
 
